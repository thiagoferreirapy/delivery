import { ProductDetailSkeleton } from "@/components/Skeleton";

// Mostrado instantaneamente ao clicar num produto (durante a navegação).
export default function Loading() {
  return <ProductDetailSkeleton />;
}
